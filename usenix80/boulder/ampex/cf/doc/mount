.sh MOUNT VIII 6/79
	...	(same description)
.s2
	When a file system which has an associated contiguous file system
(cfs) is mounted read-write [read only], the device on which the cfs resides
must also be physically present and writeable [readable].
.sh "SEE ALSO"
cfs(V)
