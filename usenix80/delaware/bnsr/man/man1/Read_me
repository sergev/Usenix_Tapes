/*% cat %
This directory contains the manual entries for software in
s1 and s2 directories of the Tilbrook Distribution tape.

DIRECTORIES:

FILES:

addr.1		addr ~ produce addresses
args.1		args ~ copy standard input to argument list and execute
badchars.1	badchars ~ find unprintable characters in files
c.1		c ~ columnate
cal.1		cal ~ print calendar
com.1		com ~ compile programs in a flexible manner
comingup.1	comingup ~ what events are in the near future
commf.1		commf ~ find fields not in dictionaries
commw.1		commw ~ print input terms not in dictionaries
cpall.1		cpall ~ copy several files to a directory
divide.1	divide ~ split files at specified pattern
dmake.1		dmake ~ automatically update large systems
double.1	double ~ take two files and outputs line that differ
ffill.1		ffill ~ fill in supressed fields
filter.1	filter ~ cat files with possible internal shell commands
findc.1		findc ~ find and replace C variables
findf.1		findf ~ find all fields that match fields in dictionary
findw.1		findw ~ select lines for printing or string substitution
grep.1		grep ~ search a file for a pattern
kmake.1		kmake ~ automatically update large systems
l.1		l ~ list files in columns
lastsl.1	lastsl ~ replace last slashes by tabs
libzer.1	libzer ~ zero out archive object files
lines.1		lines ~ print first n and last m lines
manual.1	manual ~ create manual section
medit.1		medit ~ qed mulitple files with common script
merge.1		merge ~ replace characters in master by lines from source
mvall.1		mvall ~ move a collection of files to another directory
nm.1		nm  ~  print name list
pick.1		pick ~ pick from lines or arguments to send to standard output
qed.1		qed ~ multi-file text editor
qedbufs.1	qedbufs ~ output qed saved structures
qedfile.1	qedfile ~ display or set default qed startup file for user
qmail.1		qmail ~ qed email interface
repl.1		repl ~ use grep output to replace lines
resort.1	resort ~ build new output in order specified by input
rls.1		rls ~ recursive list of files
rotf.1		rotf ~ select fields for copying
rtabs.1		rtabs ~ replace tabs by appropriate spaces
summarize.1	summarize ~ sum up and summarize on selected fields
sumup.1		sumup ~ computes totals of columns of input
suprep.1	suprep ~ suppress repeated fields
termsc.1	termsc ~ list all C variables in files
termsf.1	termsf ~ produce unique sorted list of all fields
termsw.1	termsw ~ produce list of unique terms
transl.1	transl ~ translate input fields
upd.1		upd ~ update conventionally named versions of files
wh.1		wh ~ a briefer who's on the system
whats.1		whats ~ whats 2+2
wp.1		wp ~ write protect files

PATHNAME repl change lines:

Path name change lines are not provided since there are so many
and they are not essential for operation.
