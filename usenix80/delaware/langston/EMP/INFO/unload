.TH Command : UNLOAD
.s1
The unload command is the opposite of load and follows the
exact same syntax...so why are you looking here for documentation?
type:
.EX unload (ship)
where (ship) is the ship number..

See also (instead) load
