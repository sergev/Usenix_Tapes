.th GWTAB VII 19-March-1980
.sh NAME
gwtab \*- write GRINNELL lookup table
.sh SYNOPSIS
int
.bd gwtab
(area, buff, start, nval)
.s3
int area[16];
.br
int buff[nval];
.br
int start;
.br
int nval;
.sh DESCRIPTION
.it Gwtab
is used to write  values  to  the  hardware  lookup
table.   The  values  to  be  written should be
between 0 and 4095. One to 4096 values
may  be  written to  the  table  beginning at  location
.it start
in the lookup table and continuing for
.it nval
values.
.s1
.it Area
is the buffer used in the
.it gopen
call.
.it Buff
is a table with at least
.it nval
entries, where
.it nval
is the number of values to be written.
.s3
The values are taken from
.it buff
starting at location 0, NOT location
.it start.
.s3
The value returned is non-zero if an error occurred, and zero if the call
is successful.
.sh DIAGNOSTICS
.sh FILES
/dev/gr
.sh "SEE ALSO"
gopen(VII)
.sh AUTHOR
Russ Smith
.sh BUGS
