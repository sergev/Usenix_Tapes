.th NARGS III 5/10/73
.sh NAME
nargs \*- argument count
.sh SYNOPSIS
.bd "nargs( )"
.sh DESCRIPTION
.it Nargs
returns the number of actual parameters
supplied by the caller of the routine which calls 
\fInargs\fR.
.s3
The argument count is accurate only when
none of the actual parameters is
.it float
or
\fIdouble\fR.
Such parameters count as four arguments instead of one.
.sh FILES
.lp +15 15
/lib/libc.a	archive containing standard nargs.o
.lp +15 15
/lib/nargss.o	seperated I/D space version
.i0
.sh BUGS
As indicated.
Also, this routine does not work
(as is)
in programs with separated I and D space.
Use the version in
/lib/nargss.o
if
.bd nargs
is needed in programs with separated I and D space.
Altogether it is best to avoid using this
routine and depend, for example,
on passing an explicit argument count.
