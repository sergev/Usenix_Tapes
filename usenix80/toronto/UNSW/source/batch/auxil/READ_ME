these things are useful programs for use
with the batch system. 

ctrans	converts a card deck as read in back to ordinary character file.

ptrans	converts a cyber format output to ordinary character file.

qlook	When queues get mangled this can help.

transfer When copying shared data a.outs this removes text, and uses virtual
	blocks. This saves space, and ensures that command cannot be executed
	alone.
