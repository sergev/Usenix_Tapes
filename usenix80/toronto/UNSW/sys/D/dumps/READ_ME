This directory contains command files used for managing core dump files.
On our system a core dump is 1280 blocks, so we dont like to keep too many
around.


get		Extracts a particular dump off the archive tape.

log		Contains the record of core dumps and their manipulations.

put		Append a core dump to the archive tape.

save		This extracts the dump off the dump tape as produced by
		the system. It asks all the relevant questions so that
		a tailored dumper can be made.

.skip		A program to skip forward files on tape unit.

.skip.c		Source of above.
