Files:

<e.readme, e.intro, e.macros.1, e.1, e.c, e>
A popular ed look-alike called "e".

<banner.1, banner.c, banner, banner.house>
A "banner" program which generates most characters
and which can be used as a filter (e.g. ls | banner).
Try "banner < banner.house".
