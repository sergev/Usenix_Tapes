: update unix
cp /deadstart/unix /deadstart/old-unix
cp /deadstart/unixs /deadstart/old-unixs
cp /deadstart/new-unix /deadstart/unix
cp /deadstart/new-unixs /deadstart/unixs
echo UNIX copied to OLD-UNIX, NEW-UNIX copied to UNIX
