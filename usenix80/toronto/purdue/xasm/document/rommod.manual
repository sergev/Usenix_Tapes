rommodXX is a program for makeing ROM moduales for the llXX series
linking loaders.  Usage is:

	rommodXX  moduale_name hex_moduale_address

The program will produce a file for the linking loader called
'moduale_name.rel' which will be interpreted by the linking loader
as a ROM routine. 

to use: type:  %  x/rommodXX  name  address

