Each device interpreter takes neutral device-independent code which was
generated by graphic library routines when the applications program was
run and produces a graphic displays on the corresponding device.

"mulplot", "ovr", and "rot" are filters for pipelines carrying TIG
neutral data.
