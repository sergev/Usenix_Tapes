The file "others" is like the one found in the directory "qp2". It contains
"includes" so that several routines can be compiled together. If they
are compiled separately, you get separate ".o" files which makes the command
line too long to load.
