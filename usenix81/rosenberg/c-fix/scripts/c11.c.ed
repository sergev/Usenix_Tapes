631c
		skip = 0;
	}

	if (skip)
		printf("\tL%d\nL%d:", lbl, skip);
	else
		printf("\tL%d\n", lbl);
.
628,629c
		skip = prins(op, c, branchtab, lbl);
	else {
.
625a
	register skip;
.
240a
	return skip;
.
236,237c

			if (ip != jmijne)
				printf(ip, lbl);
			else {
				skip = isn++ ;
				printf(ip, skip) ;
			}

			return skip;
.
230a
	skip = 0;

.
229a
	register skip;
	extern char jmijne[];
.
225c
prins(op, c, itable, lbl)
.
w c11.c
