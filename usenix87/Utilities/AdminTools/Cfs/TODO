- add signal handler to clean up temp file on breakout or bomb

- make checking parts of status optional, e.g. don't check modify time
  on /etc/passwd, but check everything else.

- Add ifdefs for SysV and others
