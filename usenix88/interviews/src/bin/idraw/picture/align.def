definition module align;

from main import
    Picture, XCoord, YCoord, BoundingBox;

export qualified
    aligndx, aligndy, AlignVertCenters, AlignHorizCenters, AlignCenters,
    AlignLeftSides, AlignRightSides, AlignTops, AlignBottoms, AlignLeftToRight,
    AlignRightToLeft, AlignBottomToTop, AlignTopToBottom;

(* public *) var aligndx : real;
(* public *) var aligndy : real;
(* public *) procedure AlignVertCenters(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignHorizCenters(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignCenters(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignLeftSides(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignRightSides(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignTops(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignBottoms(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignLeftToRight(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignRightToLeft(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignBottomToTop(
    const fixedPict : Picture;
    const movedPict : Picture
);
(* public *) procedure AlignTopToBottom(
    const fixedPict : Picture;
    const movedPict : Picture
);

end align.
