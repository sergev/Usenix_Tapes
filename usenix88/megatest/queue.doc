
// Class Queue impelements queues of pointers.  Can be used
// as FIFO (use method "append") or LIFO (use method "push").
//
// Public methods are
//

  void* push(void*);
  void* pop();
  void* append(void*);
  void* first();
  void* last();
  inline int length();
